export enum LOCAL_STORAGE_KEY {
  USER = 'user',
  LANG = 'lang',
  CART = 'cart_key',
  LAYOUT_TYPE = 'layout_type',
  ACCESS_TOKEN = 'accessToken',
  REFRESH_TOKEN = 'refreshToken',
  THEME = 'theme',
  CONTENT = 'note_content',
  TITLE = 'note_title',
  LAST_SAVED = 'note_last_saved',
}
